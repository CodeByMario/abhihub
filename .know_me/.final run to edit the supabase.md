**[Task Type]: B**

Two small fixes: backend validation guard + improved log line.

Edited app.py

Done. The upload route now hard-rejects any POST without a valid `subject_id`.

**Full data flow is now enforced end-to-end:**

```
UI cascade → subject UUID selected
  ↓ bulk_upload.js saveMetaModal() → stores UUID in item.meta.subject_id
  ↓ buildFormData() → appends subject_id + semester to POST
  ↓ app.py → reads + validates subject_id (400 if missing)
  ↓ save_file_record() → uses UUID directly, skips fuzzy match
  ↓ documents.subject_id = <valid uuid>
```

**Post-migration SQL checks to run (in order):**

```sql
-- 1. Confirm no nulls immediately after first test upload
SELECT id, subject_id, department_id, college_id
FROM abhihub.documents ORDER BY created_at DESC LIMIT 5;

-- 2. Confirm subject relationship is real
SELECT d.id, s.name, s.subject_code, s.semester
FROM abhihub.documents d
JOIN abhihub.subjects s ON d.subject_id = s.id
ORDER BY d.created_at DESC LIMIT 10;

-- 3. Monitor this number — should stay 0
SELECT COUNT(*) FROM abhihub.documents WHERE subject_id IS NULL;
```

**Ready to execute `008_all_features.sql`.**