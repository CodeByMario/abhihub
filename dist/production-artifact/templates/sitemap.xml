<?xml version="1.0" encoding="UTF-8"?>
<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">
{% for url in urls %}
  <url>
    <loc>{{ url.loc }}</loc>
    {% if url.lastmod %}
    <lastmod>{{ url.lastmod[:10] }}</lastmod>
    {% endif %}
    {% if url.priority %}
    <priority>{{ url.priority }}</priority>
    {% endif %}
    {% if url.changefreq %}
    <changefreq>{{ url.changefreq }}</changefreq>
    {% endif %}
  </url>
{% endfor %}
</urlset>
