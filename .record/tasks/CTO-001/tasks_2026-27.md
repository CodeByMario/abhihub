## TASK-0001

Timestamp:
2026-07-03 11:00:00 UTC

Title:
Reorganize project and create lightweight requirements

Assigned By:
CEO-001

Assigned To:
CTO-001

Priority:
High

Status:
In Progress

Deliverables:
- Identify heavy/unused dependencies and produce `requirements-min.txt`
- Break `app.py` into `routes/`, `services/`, and `utils/` modules
- Implement lazy initialization for external clients (Supabase, Firebase)
- Add a lightweight dev run script and update `Procfile` for production
- Update `README.md` with run instructions

Completion:
30%

Remarks:
Initial analysis completed; created `requirements-min.txt` and `CONTRIBUTING.md` for lightweight dev runs. Next: extract core routes into `routes/` and implement lazy service adapters.
Current step:
- Implemented Tailwind input file `static/css/tailwind-input.css` and added npm build instructions to `CONTRIBUTING.md` to standardize the CSS pipeline.
Next step:
- Added placeholder `static/css/tailwind.min.css` to avoid 404s when the built file is absent. Will be replaced by `npm run build:css`.
Progress:
- Ran `npm install` and `npm run build:css` to generate `static/css/tailwind.min.css` from `static/css/tailwind-input.css`. Build succeeded.

Notes:
- `npm audit` reports 2 vulnerabilities; consider running `npm audit fix` and updating `caniuse-lite` with `npx update-browserslist-db@latest`.

Completion:
45%
